#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define in(n) ll n;cin>>n
#define loop(r) for(int i=0;i<r;i++)

void solve(){
    in(r);
    loop(r){
        in(n);in(m);
        if (n==1 && m==1){cout<<-1;}
        else{
            int a[n][m];
            for(int i=0; i<n*m; i++){
                cin>>a[]
            }
        }
    }
}

int main(){
    solve();
}